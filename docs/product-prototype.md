# FlowWeave MVP 产品原型

浏览器原型入口：[打开 FlowWeave MVP 产品原型](./prototype/index.html)

## 产品范围

FlowWeave MVP 聚焦 Agent 节点资产、模型配置、流程编排和流程运行四个核心模块。产品当前不引入用户、组织和权限界面；运行由服务端统一创建、调度并持久化。

## 已确认的核心规则

- 节点资产按目录分类管理；节点定义模型、默认 Skill、提示词、Skill/MCP/Hooks 能力以及输入输出定义。
- MCP 与 Hooks 能力配置支持 JSON 和 YAML；Skill 使用 ZIP 导入并解析 `SKILL.md`。
- 流程通过可拖拽画布编排，节点可配置多条开始门禁和结束门禁；脚本门禁同时支持 JavaScript 与 Python。
- 每次运行保存独立流程快照，不受节点编辑态影响；用户可明确选择将最新节点配置同步到运行，历史执行和产物保持不变。
- 流程运行按“流程编排 → 运行实例”两级结构管理。
- 节点是否可运行取决于输入产物是否满足输入定义与开始门禁，不依赖上游节点状态。
- 节点输入可以引用上游产物版本，也可以由人工提供；流程可从任意节点重新开始。
- 节点修订默认在当前节点创建新的 Attempt。所有输出按版本保存，下游节点明确引用具体产物版本，历史版本不被覆盖。

## 原型文件

- `docs/prototype/index.html`：可直接在浏览器中查看的完整原型。
- `docs/prototype/flowweave-mvp.fragment.html`：原型的可编辑源片段。
